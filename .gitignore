# === VS Code SQLTools ===
.vscode/sqltoolsConnections.json
.vscode/*.sqltools*  # Any SQLTools-related settings

# === PostgreSQL ===
*.sql           # SQL dump files
*.dump          # Binary dump files
*.pgsql         # Custom Postgres dump extensions (if used)
*.log           # Database logs

# === Environment Variables ===
env/
.env
.env.*          # All variant env files (e.g., .env.dev, .env.local)

# === Docker ===
docker-compose.override.yml
docker-compose.*.yml
**/docker-data/         # If you store Postgres volumes locally
**/pgdata/              # Common Postgres volume directory
**/data/                # Custom data volumes
**/tmp/                 # Temporary files
**/logs/                # Log folders

# Docker-specific files (optional)
.dockerignore

# === OS-specific ===
.DS_Store
Thumbs.db

# === Python (if used) ===
venv/
*pyc
__pycache__/

# === Node.js (if used) ===
node_modules/


# === Database ===
*.db
